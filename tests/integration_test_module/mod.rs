/// # Integration Test Module
/// This won't be treated as an integration test, because it's not directly
/// inside the `tests` folder. This is useful to define utilities shared among
/// integrations tests.
pub fn utility_function(){}